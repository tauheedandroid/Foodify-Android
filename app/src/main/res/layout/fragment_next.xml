<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/bg_gradient"
    tools:context=".presentation.next.NextFragment">

    <!-- Title 1 -->
    <TextView
        android:id="@+id/tvcake"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cakes"
        android:textColor="@color/black"
        android:textStyle="bold|italic"
        android:textSize="24sp"
        android:layout_marginTop="10dp"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- First RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCake"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingStart="15dp"
        android:clipToPadding="false"
        android:paddingEnd="200dp"
        app:layout_constraintTop_toBottomOf="@id/tvcake"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Title 2 -->
    <TextView
        android:id="@+id/tvIceCream"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ice Cream"
        android:textColor="@color/black"
        android:textStyle="bold|italic"
        android:textSize="24sp"
        android:layout_marginTop="8dp"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toBottomOf="@id/rvCake"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Second RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvIceCream"
        android:layout_width="0dp"
        android:layout_height="250dp"
        android:paddingStart="16dp"
        android:paddingEnd="200dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/tvIceCream"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Title 3 -->
    <TextView
        android:id="@+id/tvFruit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Fruits"
        android:textColor="@color/black"
        android:textStyle="bold|italic"
        android:textSize="24sp"
        android:layout_marginTop="250dp"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toBottomOf="@id/rvCake"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- third RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvFruit"
        android:layout_width="0dp"
        android:layout_height="250dp"
        android:layout_marginTop="250dp"
        android:clipToPadding="false"
        android:paddingStart="16dp"
        android:paddingEnd="200dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvIceCream" />

</androidx.constraintlayout.widget.ConstraintLayout>